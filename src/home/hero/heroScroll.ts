/** Hero scroll progress (0 at the top, 1 once the hero has left the viewport). Written by the
 *  hero's ScrollTrigger, read every frame by the 3D scene — a plain object, no React state. */
export const heroScroll = { progress: 0 }
